
(()=>{const header=document.querySelector('[data-header]');const menu=document.querySelector('[data-mobile-menu]');const toggle=document.querySelector('[data-menu-toggle]');const closeMenu=()=>{if(!menu||!toggle)return;menu.classList.remove('is-open');menu.setAttribute('aria-hidden','true');toggle.setAttribute('aria-expanded','false');document.body.classList.remove('menu-open')};if(toggle&&menu){toggle.addEventListener('click',()=>{const open=!menu.classList.contains('is-open');menu.classList.toggle('is-open',open);menu.setAttribute('aria-hidden',String(!open));toggle.setAttribute('aria-expanded',String(open));document.body.classList.toggle('menu-open',open)});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));document.addEventListener('keydown',e=>{if(e.key==='Escape')closeMenu()})}const onScroll=()=>header&&header.classList.toggle('is-scrolled',window.scrollY>18);onScroll();addEventListener('scroll',onScroll,{passive:true});
const lb=document.querySelector('[data-lightbox]');if(lb){const items=[...document.querySelectorAll('.lightbox-item')];let idx=0;const lbImg=lb.querySelector('img');const show=i=>{if(!items.length)return;idx=(i+items.length)%items.length;lbImg.src=items[idx].dataset.full;lbImg.alt=items[idx].querySelector('img')?.alt||'Photo agrandie';lb.classList.add('open');lb.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'};const hide=()=>{lb.classList.remove('open');lb.setAttribute('aria-hidden','true');document.body.style.overflow=''};items.forEach((it,i)=>{it.addEventListener('click',e=>{if(e.target.closest('figcaption'))return;show(i)})});lb.querySelector('.lightbox-close')?.addEventListener('click',hide);lb.querySelector('.lightbox-prev')?.addEventListener('click',()=>show(idx-1));lb.querySelector('.lightbox-next')?.addEventListener('click',()=>show(idx+1));lb.addEventListener('click',e=>{if(e.target===lb)hide()});document.addEventListener('keydown',e=>{if(!lb.classList.contains('open'))return;if(e.key==='Escape')hide();if(e.key==='ArrowLeft')show(idx-1);if(e.key==='ArrowRight')show(idx+1)})}
const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduce){document.body.classList.add('motion-ready');const cards=[...document.querySelectorAll('.gallery-card,.service-card,.theme-card,.universe-card')];if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('motion-in');io.unobserve(e.target)}}),{rootMargin:'0px 0px -8% 0px',threshold:.04});cards.forEach(c=>io.observe(c))}else{cards.forEach(c=>c.classList.add('motion-in'))}document.querySelectorAll('.hero-visual,.subhero-collage').forEach(zone=>{zone.addEventListener('pointermove',e=>{const r=zone.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;zone.style.setProperty('--parallax-x',`${x*8}px`);zone.style.setProperty('--parallax-y',`${y*8}px`)});zone.addEventListener('pointerleave',()=>{zone.style.setProperty('--parallax-x','0px');zone.style.setProperty('--parallax-y','0px')})})}
})();
