
(()=>{const header=document.querySelector('.site-header'),progress=document.querySelector('.progress'),menu=document.querySelector('.menu-btn'),mobile=document.querySelector('.mobile-nav');function onScroll(){const y=window.scrollY;header?.classList.toggle('scrolled',y>20);if(progress){const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max>0?Math.min(100,y/max*100):0)+'%'}}addEventListener('scroll',onScroll,{passive:true});onScroll();if(menu&&mobile){menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));mobile.classList.toggle('open',!open)});mobile.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{menu.setAttribute('aria-expanded','false');mobile.classList.remove('open')}))}
const track=document.querySelector('.reviews-track');const prev=document.querySelector('[data-review-prev]'),next=document.querySelector('[data-review-next]');const step=()=>Math.min(430,track?.clientWidth*.9||350);prev?.addEventListener('click',()=>track.scrollBy({left:-step(),behavior:'smooth'}));next?.addEventListener('click',()=>track.scrollBy({left:step(),behavior:'smooth'}));let timer;function auto(){if(!track||matchMedia('(prefers-reduced-motion: reduce)').matches)return;clearInterval(timer);timer=setInterval(()=>{const end=track.scrollLeft+track.clientWidth>=track.scrollWidth-24;track.scrollTo({left:end?0:track.scrollLeft+step(),behavior:'smooth'})},4600)}if(track){track.addEventListener('mouseenter',()=>clearInterval(timer));track.addEventListener('mouseleave',auto);track.addEventListener('touchstart',()=>clearInterval(timer),{passive:true});track.addEventListener('touchend',auto,{passive:true});auto()}
const visuals=document.querySelectorAll('[data-parallax]');if(visuals.length&&!matchMedia('(prefers-reduced-motion: reduce)').matches){addEventListener('scroll',()=>{const y=scrollY;visuals.forEach((el,i)=>el.style.translate=`0 ${Math.min(24,y*(.015+i*.006))}px`)},{passive:true})}
})();
